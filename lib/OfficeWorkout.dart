import 'package:flutter/material.dart';

class OfficeWorkout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('page3', style: TextStyle(fontSize: 34)),
      ),
    );
  }
}
