import 'package:flutter/material.dart';

class HomeWorkout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('page4', style: TextStyle(fontSize: 34)),
      ),
    );
  }
}
